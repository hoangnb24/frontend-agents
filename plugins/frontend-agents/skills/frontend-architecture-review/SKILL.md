---
name: frontend-architecture-review
description: Use to review React/Vite/shadcn frontend architecture, component layering, feature boundaries, shared abstractions, route composition, and reuse decisions without editing files.
---

# Frontend Architecture Review

Review only architecture and ownership boundaries. Do not edit files unless the orchestrator explicitly changes the assignment.

Load `references/architecture-checklist.md` and return:

- Decision: `Pass`, `Advisory`, `Fix Required`, or `Blocked`.
- Evidence: `Verified`, `Partially Verified`, or `Not Verified`.
- Severity-tagged findings with file and line references.
- Required fixes.
- Residual risk.

